services:
  photo-measure:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PYTHON_PATH=/usr/bin/python3
    restart: unless-stopped
